# PIANO INTERVENTI — Problematiche CT3.0

## 1. Test R7/R8: percentuale undefined per III.A

**Problema**: I test R7 e R8 usano III.A (Pompa di calore), ma `FormulaEngine.calculate()` non espone `params.percentuale` per codici con `tipo_formula: "prestazionale"`. La percentuale come cap viene calcolata internamente ma non esposta nel return.

**Soluzione applicata**: Sostituire III.A con II.A (Isolamento) nei test R7/R8. II.A è `tipo_formula: "percentuale_spesa"` ed espone correttamente `params.percentuale` risolto da `_resolvePercentuale()`:
- Privato II.A Zona E → 50% (cap 65%)
- PA ≤15k II.A → 100% (comuneSotto15k)
- ETS non econ II.A → 100% (assimilato PA)
- Impresa II.A singolo → 25% (non 45% che è per Titolo III)

**Alternativa futura**: Se serve testare III.A, leggere `res.params` completo per capire quali campi sono esposti, o modificare `_executeGenericFormula` per esporre sempre `params.percentuale`.

## 2. Test R7-5: II.D dati insufficienti

**Problema**: II.D richiede `tipo_intervento_nzeb`, `volume_esistente_mc`, `volume_nuovo_mc` passati come `tipo_intervento` (nome campo errato).

**Soluzione applicata**: Rinominare `tipo_intervento` → `tipo_intervento_nzeb`, aggiungere `volume_esistente_mc: 500` e `volume_nuovo_mc: 100`.

## 3. PROBLEMATICHE.md: copertura test ✅

Verificata: ogni regola ha almeno un test corrispondente. Totale 51 test unitari + 8 pratiche JSON. Aggiornato conteggio in PROBLEMATICHE.md (46→51).

## 4. PROBLEMATICHE.md: allineamento riferimenti righe ✅

Verificati 50 riferimenti in 4 file. Correzzioni:
- `normativa.js` riga 573→572 (privato_terziario.titolo_ii)
- `formula_engine.js`: 12 riferimenti obsoleti aggiornati alle linee correnti (284, 288-291, 297-300, 302-303, 341-342, 353-354, 267, 278, 1191-1193)
- `_resolveBonusScuole()` rimosso (logica inline)
- `cross_rule_engine.js` e `rules_engine.js`: tutti i riferimenti ancora validi

## 5. test_problematiche.html: manutenzione

**Da fare**: Quando si aggiungono nuove regole o si modificano quelle esistenti:
1. Aggiornare `docs/PROBLEMATICHE.md`
2. Aggiungere test in `static/test/test_problematiche.html`
3. Verificare con `npx live-server --port=8080 --no-css-inject` da `static/`
4. Aprire `http://localhost:8080/test/test_problematiche.html`
5. Commit delle modifiche

## 6. Estensione futura: test automatici in CI

**Idea**: Se in futuro si introduce CI, i test in `test_problematiche.html` e `test_suite.html` potrebbero essere eseguiti con un headless browser (Puppeteer/Playwright) per automazione.
